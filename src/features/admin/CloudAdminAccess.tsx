import { FormEvent,useState } from "react";import{Eye,EyeOff,KeyRound,ShieldCheck}from"lucide-react";import{authService}from"../../auth/authService";import"./admin.css";
const ADMIN_EMAIL="comercial@roys.com.br";
export function CloudAdminAccess({onAuthenticated,onBack}:{onAuthenticated:()=>void|Promise<void>;onBack:()=>void}){const[password,setPassword]=useState("");const[show,setShow]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[sent,setSent]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await authService.signInAdmin(ADMIN_EMAIL,password);await onAuthenticated()}catch{setError("E-mail ou senha administrativos incorretos.")}finally{setBusy(false)}}async function recover(){setBusy(true);setError("");try{await authService.requestAdminPasswordReset(ADMIN_EMAIL);setSent(true)}catch{setError("Não foi possível enviar a recuperação agora.")}finally{setBusy(false)}}return <main className="admin-access-shell"><section className="admin-access-brand"><button className="admin-back-button" onClick={onBack}>Voltar</button><div className="admin-brand-lockup"><img src="/brand/logo-primary-black.png" alt="Roy's"/><div><span>Roy's Controle</span><strong>Administração operacional</strong></div></div><div className="admin-access-promise"><span className="admin-access-index">Acesso mestre</span><h1>Decisões claras.<br/><em>Dados protegidos.</em></h1><p>Gerencie lojas, funcionários, fechamentos, métricas e correções.</p></div><div className="admin-security-note"><ShieldCheck/><div><strong>Acesso centralizado</strong><span>Recuperação vinculada ao e-mail comercial.</span></div></div></section><section className="admin-access-form-panel"><form className="admin-access-form" onSubmit={submit}><div className="admin-form-icon"><KeyRound/></div><span className="admin-form-eyebrow">Área protegida</span><h2>Acesso administrativo</h2><p>{ADMIN_EMAIL}</p><label className="admin-password-field"><span>Senha administrativa</span><div><input autoComplete="current-password" type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" aria-label={show?"Ocultar senha":"Mostrar senha"} onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p role="alert" className="admin-form-error">{error}</p>}{sent&&<p className="admin-form-success">Enviamos as instruções para {ADMIN_EMAIL}.</p>}<button className="admin-submit" disabled={busy}>{busy?"Entrando…":"Entrar no painel"}</button><button className="admin-recovery-link" type="button" onClick={()=>void recover()} disabled={busy}>Esqueci minha senha</button></form></section></main>}
